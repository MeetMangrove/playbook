
sudo: false
language: ruby
rvm:
  - "2.2"
install:
  - gem install travis --no-rdoc --no-ri
script:
  - eval "$(curl -s https://raw.githubusercontent.com/balupton/awesome-travis/master/scripts/travis-another.bash)"
# travis env set OTHER_REPO_SLUG 'MeetMangrove/playbook-website' --public
# travis env set GITHUB_TRAVIS_TOKEN "$GITHUB_TRAVIS_TOKEN"
# travis encrypt "mangrovefriends:$SLACK_TRAVIS_TOKEN#updates" --add notifications.slack